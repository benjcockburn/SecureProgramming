#include "login.h"


std::string login::seach_User(std::string user){
    std::string password;
    //if user is in user storage return the stored encryped password
    return password;
}
bool login::check_Password(std::string user,std::string pWord){
    //first we check if the user is real then have user enter the secure key that is not stored in code
    std::string password = seach_User(user);
    std::cout<<"Enter key"<<std::endl;
    //the entered password is encrypted using the entered key
    std::string userPass;

    //then we compare and return true or false
    





}